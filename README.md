# Encryption-Decryption-Tool
A Java encryption and decryption tool that securely transforms messages using a configurable character key. The program reads and writes external text files, separates encryption logic from execution, and ensures accurate message recovery through reversible transformations.
